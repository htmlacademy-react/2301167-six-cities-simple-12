// import { OPTIONS_SORTING } from './const';
// import { getSortingBy } from './general';
// import { makeFakeOffers } from './utils/mocks';

// const fakeOffers = makeFakeOffers();
// const offers = [...fakeOffers];

export {};
describe('Function: getSortingBy', () => {
  test('should return sorted "offers" by Price: low to high', () => {
    // const offers = [...fakeOffers];
    // expect(getSortingBy(offers, OPTIONS_SORTING[1])).toEqual([...offers]);
  });
});
